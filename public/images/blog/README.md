# Blog covers

Unique 16:9 editorial JPG covers (one per post), named `{slug}.jpg`.

Register each cover in `lib/blog-covers.ts`.

Do **not** run the old SVG generator — covers are hand-curated realistic images per post.
